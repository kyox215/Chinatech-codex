# OPTIONAL USER-SCOPED SETTINGS
# Merge the desired keys into ~/.codex/config.toml; do not copy blindly over an
# existing user configuration. Native Memories are an auxiliary layer only.

[features]
hooks = true
memories = true

[memories]
generate_memories = true
use_memories = true
disable_on_external_context = true
