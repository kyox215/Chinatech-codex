# AI Company OS — project-scoped Codex configuration
# This file intentionally does not pin a model, provider, credentials, or MCP server.
# Those choices remain under the operator's control.

project_doc_max_bytes = 32768

[features]
hooks = true

[agents]
max_threads = 6
max_depth = 1
job_max_runtime_seconds = 1800
