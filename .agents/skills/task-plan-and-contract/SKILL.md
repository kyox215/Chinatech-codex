---
name: task-plan-and-contract
description: 用于标准以上任务，在实施前生成阶段计划、工作包、依赖、验证、回滚和变更合同。
---

# task-plan-and-contract

## 目标

建立“做什么、谁做、如何证明、失败怎么退”的执行合同，使实施可以分阶段验收和恢复。

## 输入

- 任务章程与验收标准
- 探索/架构结果
- 风险与批准边界
- 资源、依赖和环境约束

## 执行流程

1. 按业务能力而非文件数量拆分阶段；每阶段产生可验证增量和明确退出条件。
2. 建立工作分解：owner、输入、输出、依赖、涉及文件、预计风险、测试和证据。
3. 区分调查、决策、实施、迁移、验证、发布和记忆更新，避免把它们混成“开发完成”。
4. 定义变更预算：允许修改的模块、禁止触碰区域、最大迁移范围、兼容窗口和无关重构限制。
5. 对数据/API/状态机变更定义向后兼容、双写/回填、切换与回滚顺序。
6. 把每个验收标准映射到验证步骤和证据位置，指定 QA/Security 的独立复核点。
7. 计划改变时记录 Plan Delta：原因、影响、风险重分类、是否需要重新批准。

## 必须产出

- 分阶段执行计划
- 工作包与依赖
- 文件影响清单
- 测试/证据矩阵
- 回滚与暂停条件

## 门禁与停止条件

- 未映射验收标准的工作不进入实施
- 高风险阶段无回滚/备份验证不得启动
- 计划不是事实，实际结果必须另行记录

## 记忆更新

更新 TASK.md、CHECKPOINTS.md 初始计划和 ACTIVE_CONTEXT 下一阶段。

## 通用执行约束

- 先读当前目录适用的 `AGENTS.md` / `AGENTS.override.md`。
- 只加载与任务相关的政策和记忆；避免把整个制度包塞入上下文。
- 对事实附文件、符号、测试、日志或正式决定证据；不把推断写成事实。
- 遵守单一写入者、最小权限、可逆优先、秘密最小化和不覆盖用户改动。
- 当范围、风险、目标环境或关键假设变化时，暂停并重新分类。

## 建议输出格式

```markdown
# 结论
# 已验证事实与证据
# 假设 / 未知 / 冲突
# 决策或建议
# 风险与审批点
# 验证结果
# Memory Delta
# 下一步
```
