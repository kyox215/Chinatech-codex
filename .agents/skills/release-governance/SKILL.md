---
name: release-governance
description: 用于客户可见、生产、包发布、迁移或基础设施变更，生成发布、观测、回滚和批准方案。
---

# release-governance

## 目标

把“代码完成”转换为可控上线，确保版本、环境、监控、沟通、迁移和恢复均有 owner。

## 输入

- 通过的质量/安全证据
- 变更范围
- 目标环境
- 迁移与依赖
- SLO/监控和批准边界

## 执行流程

1. 确认发布单元、版本、目标环境、依赖顺序和冻结范围；验证不是误指向生产。
2. 建立发布前清单：构建、测试、安全、配置、秘密、迁移、备份、容量、兼容和客户支持。
3. 选择渐进策略：预览、内部、金丝雀、分批、功能开关或蓝绿，并定义扩量门槛。
4. 定义发布健康指标、日志、告警、业务指标、观察窗口和负责人员。
5. 写出可执行回滚/前滚步骤、触发阈值、数据恢复限制和通讯模板。
6. 对生产写入、公开发布和外部通知取得所需批准；Agent 不自行执行保留决策。
7. 发布后验证真实用户路径与数据，记录结果；异常进入 `$incident-response`。

## 必须产出

- 发布计划
- 门禁清单
- 观测与扩量标准
- 回滚运行手册
- 批准与发布记录

## 门禁与停止条件

- 无明确目标环境不得部署
- 数据库不可逆变化不能承诺简单回滚
- 发布成功必须有运行证据

## 记忆更新

更新 release/platform 部门记忆、发布记录、任务证据和已知限制。

## 通用执行约束

- 先读当前目录适用的 `AGENTS.md` / `AGENTS.override.md`。
- 只加载与任务相关的政策和记忆；避免把整个制度包塞入上下文。
- 对事实附文件、符号、测试、日志或正式决定证据；不把推断写成事实。
- 遵守单一写入者、最小权限、可逆优先、秘密最小化和不覆盖用户改动。
- 当范围、风险、目标环境或关键假设变化时，暂停并重新分类。

## 建议输出格式

```markdown
# 结论
# 已验证事实与证据
# 假设 / 未知 / 冲突
# 决策或建议
# 风险与审批点
# 验证结果
# Memory Delta
# 下一步
```
