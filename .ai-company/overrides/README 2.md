# Nested AGENTS Examples

Copy only the applicable `.example` file into a real code subtree as `AGENTS.md`.
Codex merges root-to-current-directory instructions; closer files should contain
only area-specific additions or overrides. Keep the combined instruction budget
small and avoid repeating the root constitution.
