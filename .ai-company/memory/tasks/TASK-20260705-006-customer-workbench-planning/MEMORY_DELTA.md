# Memory Delta

Candidate project memory after implementation approval:

- Customer management direction should prioritize a device-centered customer detail workbench. Outer customer list cards should stay lightweight; full customer/device/order/statistics context belongs inside `/customers/[id]`.
- First implementation phase should derive device-order grouping from existing `CustomerDetail` without schema migration, then refresh the customer detail IA.

Not yet permanent:

- Owner has not confirmed the decision options.
- No code implementation has started.
