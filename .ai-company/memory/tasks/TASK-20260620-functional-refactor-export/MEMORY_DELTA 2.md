# Memory Delta

Candidate project memory:

- When the owner asks for all project information for refactor and explicitly excludes UI design, produce an independent functional refactor handoff package, not a UI redesign package.
- Include system functions, routes, runbook, API/data contracts, migrations, tests, scripts, and refactor risks.
- Exclude `.env.local`, secrets, screenshots, Figma, visual tokens, and UI design guidance.
- In dirty RepairDesk checkouts, isolate export artifacts under `exports/` and do not normalize unrelated files.
