# Memory Delta

- Customer lookup inline results should render below the new-order field shell, not inside the phone field content column.
- Mobile matched-customer customer lookup requires explicit visual/E2E coverage because the empty-result state alone can pass while the matched panel regresses.
