# Memory Delta — TASK-20260720-002

- Safari/WebKit `!important` cascade layers must be tested by computed print-media styles; source-order inspection alone is insufficient.
- Customer print documents must not contain internal task routes or internal UUID QR shortcuts; staff scanning requires a separate staff-only print contract.
- Dirty navigation must sequence dialog detach and safe pointer-lock release before router/history execution; closing animations are part of the interaction state machine.
- New-order prefill should be route-keyed and abort stale fetches; repeated intake actions need a non-sensitive session identity.
- No capability or autonomy promotion is authorized by this task.
