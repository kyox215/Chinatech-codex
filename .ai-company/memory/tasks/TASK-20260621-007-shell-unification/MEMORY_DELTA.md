# Memory Delta

Candidate long-term project memory:

- Workspace shell navigation and global quick actions now belong in `src/shared/config/navigation.ts`.
- Shell action execution belongs in `src/shared/lib/shell-actions.ts`.
- Mobile RepairOS workspace routes should not show the global AppBar together with the page-level floating header.

No secrets, production credentials, or customer PII were recorded.
