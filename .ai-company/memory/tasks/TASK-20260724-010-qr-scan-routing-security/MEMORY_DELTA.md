# Memory Delta

- Customer status QR is a dedicated sensitive scan payload and must never enter generic business search or persisted list state.
- Public and staff resolution of a scanned token share one generation; stale responses cannot navigate.
