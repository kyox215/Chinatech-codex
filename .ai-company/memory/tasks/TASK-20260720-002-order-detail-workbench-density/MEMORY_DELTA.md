# Memory Delta

## Candidate project memory

- Desktop order detail should be treated as a bounded workbench rather than a viewport-width canvas: use a 1040px shell capped around 720px high, 1000px primary content, and 920px low-frequency record/photo/cost content.
- Repeated financial summaries should have one primary location in the hero; the bottom dock should retain only settlement status and actions.
- Role-specific columns should use bounded track widths and `items-start` so sparse cards do not visually stretch to match the tallest device card.

## Not changed

- No API, data contract, permission, workflow, payment, notification, custody, or mobile behavior changed.
