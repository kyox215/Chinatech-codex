# 商品库存手机端高密度完整实施规划

## 目标体验

移动端采用“核心字段永远可见、低频字段渐进展开、动作固定可达”的单页工作台：列表一眼扫过身份/状态/售价/库位，录入与编辑在键盘打开时仍能保存，详情把六项规格压缩成两行且不重复设备标识。桌面保留六列表格与完整工作区。

## 交付阶段

1. **安全合流**：以 `b2598713` 恢复库存/回收业务树，重放 `a9e6db44` 保留维修选项。
2. **列表高密度**：84–88px 卡片、36px 类别图形、三行主体、状态/金额右栏、查询切换保留上一结果。
3. **录入高密度**：五列类别、品牌/型号双列、规格双列、主标识全宽、固定双动作栏。
4. **详情高密度**：56px Hero 图形、三列规格、身份两列、标识去重、旧 DTO 兼容。
5. **编辑高密度**：紧凑信息卡、类别相关字段、固定动作栏、字段级错误，保留 CAS 和敏感标识边界。
6. **验证与发布**：专项测试、全量门禁、双浏览器多视口截图、独立 QA、preview、production exact-SHA smoke。

## 后续独立路线

- R3：预留、售卖、退货、报废和服务端 `allowed_actions`。
- R3：cursor 分页、数据库过滤/排序和真实规模性能证据。
- R3：字段级三方冲突协议与成本清空财务语义。
- T3：全站 density 分支专门集成。
- 治理：建立 production manifest/release train，避免落后 `main` 自动部署造成能力回退。
