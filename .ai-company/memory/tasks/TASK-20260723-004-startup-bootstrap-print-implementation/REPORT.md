# 启动性能、打印准备度与工单工作区集成发布报告

## 结论

TASK-004 与已关闭的 TASK-005、TASK-006 作为一个集成发布单元完成验证并发布到生产。业务提交 `274dc50f` 已推送 `main`，Vercel 生产部署 `dpl_Gx8EapZ7xGaF6QmewhU3R9YSutYW` 状态为 `Ready`。

## 交付结果

- Shell 冷启动改为单一 bootstrap 快照，并保留仅针对缺失端点的旧接口兼容回退。
- 首页首屏不再被无关跨业务预加载争抢；AI 与后台预加载延后到合适时机。
- 单张打印与批量打印/导出权限分开投影，打印禁用提供真实原因和恢复入口。
- 概览快速接单复用工单模块的共享新建弹窗。
- 跨模块普通新建、详情入口统一为可刷新恢复的 `/orders` 工作区意图；独立深链、任务页和移动详情保持原语义。
- 项目级启动性能与打印准备度声明已写入，并纳入后续页面、API 与发布门禁。

## 验收矩阵

| 验收项 | 结果 | 证据 |
|---|---|---|
| 首屏前不触发无关预加载 | 通过 | E-002、E-012 |
| Shell 单 actor bootstrap 与兼容回退 | 通过 | E-002、E-003、E-012 |
| Manager 单张打印、批量/导出受限 | 通过 | E-004、E-010、E-012 |
| 打印禁用原因与恢复入口 | 通过 | E-005、E-009、E-016 |
| 概览/工单工作区入口一致 | 通过 | E-013、TASK-005/006 报告 |
| lint、typecheck、全量测试、build | 通过 | E-012 |
| Chromium/WebKit 自动化打印路径 | 通过 | E-013 |
| main 推送与生产部署 | 通过 | E-014 |
| 生产登录边界与页面冒烟 | 通过 | E-015、E-016 |

## 范围控制

未完成的 TASK-007、临时打印审计文件、旧任务被测试重写的截图/PDF、`next-env.d.ts` 和测试生成的旧截图均未进入业务发布提交。没有数据库迁移、依赖升级、生产数据写入或权限模型扩大。

## 残余风险与后续

- Safari 原生系统打印预览与门店 HP 实体纸张仍需实机关闭，Owner 为门店运营/Integration Lead，跟踪任务为 TASK-007。
- 自动化证明 DOM 隔离、二维码准备和 `window.print()` 调用路径，但不替代物理打印机、纸张缩放与二维码实扫验收。
- 若出现登录循环、跨店缓存、角色打印矩阵或工作区恢复异常，立即回滚到上一 Ready production deployment：`chinatech-codex-7n7epauk3-kyox120-9295s-projects.vercel.app`。

## 操作说明

- 员工从概览、客户、利润、扫码或命令面板进入普通工单时，会回到统一工单工作区并打开共享弹窗。
- 打印按钮不可用时按页面提示补齐店铺资料、QR 配置或权限；批量打印仍只向拥有导出能力的角色开放。
