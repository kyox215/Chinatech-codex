# Memory Delta

- IMEI scanner camera permission caching must not store media, IMEI, device IDs or customer data; only store a non-sensitive last working mode.
- Active camera mode changes must not be a dependency that restarts the camera effect.
