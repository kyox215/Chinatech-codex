# Memory Delta

- Candidate reusable rule: never expose the application shell when the root stylesheet is unavailable.
- Candidate reusable rule: web fonts must be self-hosted; no runtime external font stylesheet in the critical CSS path.
- Both candidates are verified by production build plus Chromium/WebKit tests, but remain task-level candidates pending a second reuse case.
- Not promoted to shared project/department memory in this turn because the relevant long-term memory files contain unrelated concurrent uncommitted changes.
- No capability or autonomy level changed.
