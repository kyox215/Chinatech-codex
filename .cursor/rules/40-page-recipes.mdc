---
description: Next.js 页面骨架与 RepairOS 页面配方
globs: src/app/**,src/features/**/screens/**
---

# 页面骨架配方

每个新页面必须：

1. `src/app/*` 保持 thin route。
2. 业务主体放 `src/features/*/screens`。
3. 使用 `@/lib/ui-patterns` 和 `@/lib/component-patterns`。
4. 入场动画只从 `@/lib/motion` 取。
5. 移动端业务详情遵守 `docs/REPAIROS_MOBILE_DETAIL_STANDARD.md`。

## App Router Route

```tsx
import { CustomersScreen } from "@/features/customers/screens/customer-list-screen";

export const metadata = {
  title: "客户 — RepairDesk",
  description: "客户档案与维修历史",
};

export default function CustomersPage() {
  return <CustomersScreen />;
}
```

## 列表页

使用 RepairOS list scaffold 或现有 feature screen pattern：

- 顶部状态栏固定但不压内容。
- 搜索和分组 chips 不横向溢出。
- 移动端卡片高密度，桌面端可切表格或多列。
- 空态、加载态、错误态都要有。

## 移动详情 / 工作流页

必须优先使用：

- `repairOs.mobileFloatingPage`
- `repairOs.mobileFloatingHeaderShell`
- `repairOs.mobileFloatingHeaderCard`
- `repairOs.mobileInfoCard`

信息顺序按任务场景组织：状态/下一步、客户、设备、故障/检测、报价/支付、附件/历史、底部高频动作。

## Dialog / Sheet

- 小表单使用单层 modal 容器。
- 复杂业务详情使用沉浸式工作面或 RepairOS bottom sheet。
- 滚动必须在明确 body 区域。
- 不允许 Dialog 外盒 + glass-card + card 多层套壳。
