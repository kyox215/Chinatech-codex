---
description: Next.js / Supabase / Query 技术栈硬性约定
alwaysApply: true
---

# 技术栈约定

## 路由

- 使用 Next.js App Router。
- 路由文件位于 `src/app/`。
- `src/app/*` 只放 route、metadata、layout、route handlers。
- 业务页面主体放 `src/features/*/screens`。
- 禁止新增 TanStack Router/Start、Vite entrypoints、`src/routes`、`routeTree.gen.ts`。

## 数据

- 客户端用 `@tanstack/react-query`。
- Query key 优先用 feature key factory，例如 `ordersKeys`、`customersKeys`、`inventoryKeys`。
- App data facade 使用 `@/lib/repairdesk/api`。
- Mock 和 server contract 必须一致。

## 服务端逻辑

- API route handlers 放 `src/app/api/*`。
- Server service/repository 放 `src/features/*/server` 或 `src/server/*`。
- Client component 不得导入 server-only 模块。
- 权限、付款、库存、客户 PII、状态流转必须服务端校验。

## 数据库 / Supabase

- schema 变更必须用 `supabase/migrations/*`。
- destructive SQL 需要用户明确授权，并由主线程执行。
- 暴露 schema 的表必须考虑 RLS。
- service role 只能在受控服务端使用，不能进入浏览器 bundle。

## 验收

默认运行：

```bash
npm run lint
npm run typecheck
npm run test
npm run build
```

如果 `npm run check` 覆盖全部门禁，也可以使用它。
