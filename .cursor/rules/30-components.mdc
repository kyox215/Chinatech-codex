---
description: 业务组件、shadcn 原子组件与 API 使用规则
globs: src/components/**,src/features/**,src/shared/**,src/app/**
---

# 组件速查

## shadcn 原子组件

- 复用 `src/components/ui/*`。
- 可以新增必要 variant，但不要为单个页面随意改全局 primitive 行为。
- Dialog/Sheet/Popover 使用 `src/lib/component-patterns.ts` 的统一 overlay/body/section 声明。

## RepairOS 业务组件

优先复用：

- `src/shared/ui/repair-os-mobile.tsx`
- `src/components/orders/badges.tsx`
- `src/lib/ui-patterns.ts`
- `src/lib/component-patterns.ts`
- `src/lib/motion.ts`

## 业务渲染

- 金额优先用 `<MoneyText />`。
- 电话优先用 `<PhoneText />`。
- 订单状态/类型优先用现有 badge 组件。
- 图标用 `lucide-react`。

## 数据获取

```tsx
const { data = [] } = useQuery({
  queryKey: ordersKeys.list(filters),
  queryFn: () => listOrders(filters),
});
```

规则：

- Client 只走 `@/lib/repairdesk/api` 或 feature API facade。
- 不从 client component 导入 `src/server/*`。
- Mutation 成功后 invalidate 相关 query。
- 关键业务规则不能只靠前端限制，必须服务端校验。
