---
description: Next.js App Router 外壳、导航与内容区规则
alwaysApply: true
---

# 应用外壳

根布局由 `src/app/layout.tsx` 和共享 app shell 控制。新路由文件必须保持 thin：只导入 feature screen，不写业务逻辑。

## 路由页面模板

```tsx
import { SomeFeatureScreen } from "@/features/some-feature/screens/some-feature-screen";

export const metadata = {
  title: "页面名 — RepairDesk",
  description: "页面说明",
};

export default function Page() {
  return <SomeFeatureScreen />;
}
```

## 导航规则

新增导航页面必须同步：

- `src/components/app-sidebar.tsx`
- `src/components/app-bar.tsx` 的 breadcrumb/label
- `src/components/command-palette.tsx`
- `src/shared/config/navigation.ts` 或现有导航配置

## 移动工作台规则

- 移动端高频业务详情使用 RepairOS Floating Card。
- 顶部状态卡不要写成整屏固定白色栏。
- 复杂详情页必须测量顶部卡高度并给正文让位。
- 底部动作条不得遮挡内容。
- 真实输入字号保持 16px 以上，避免移动端键盘触发页面缩放。

## 内容容器

优先使用 `src/lib/ui-patterns.ts` 中的 `repairOs.*`、`pageShell.*`、`detailWorkspace.*` 等声明，不在页面里手写一套宽度、阴影、颜色系统。
