---
description: RepairDesk 项目总览、架构红线与 Agent 入口
alwaysApply: true
---

# RepairDesk · 项目总览

RepairDesk 是手机维修店工作台。当前技术栈：**Next.js App Router + React 19 + Tailwind v4 + shadcn/ui + framer-motion + TanStack Query + Supabase**。

设计语言：**RepairOS Floating Card / 高密度移动工作台 / token 化白色业务表面**。当前移动订单详情页是移动业务详情的视觉源头。

## 必读入口

非简单任务先读：

1. `AGENTS.md`
2. `AI智能部门管理/部门化管理设计.md`
3. `.agents/README.md`
4. 相关页面/组件/响应式声明文档

## 红线

1. **不写死颜色**。禁止新增 hex/rgb、`text-white`、`bg-black` 等硬编码颜色；统一走 `src/styles.css` 语义 token。
2. **使用 Next.js App Router**。路由文件放 `src/app/`，页面业务主体放 `src/features/*/screens`。
3. **不重新引入 TanStack Router/Start、Vite entrypoints、`src/routes` 或 `routeTree.gen.ts`**。
4. **client component 不得 import `src/server/*`**；数据读写走 `@/lib/repairdesk/api` 或 feature API facade。
5. **新增业务页面遵守 RepairOS 标准**，尤其是移动详情/任务/报价/拍照/支付页面。
6. **多 Agent 工作必须由主线程集成**；子代理默认只读，写入必须有明确且不重叠的文件范围。
7. **敏感数据不外泄**。客户 PII、附件、支付、Supabase service role、环境变量不得写入日志、URL、文档或浏览器公开存储。

## 项目结构速记

```txt
src/
  app/                         Next.js routes, metadata, layouts, route handlers
  features/
    orders|customers|inventory|buyback|...
      screens/                 page bodies
      components/              feature UI
      model/                   pure business rules
      server/                  repositories and services
      testing/                 mocks and fixtures
  components/ui/               shadcn primitives
  shared/                      shared UI/config/lib
  lib/
    repairdesk/api.ts          app data facade
    ui-patterns.ts             reusable layout classes
    component-patterns.ts      reusable component classes
    motion.ts                  motion source of truth
  styles.css                   only design token source
```

## 工作流

- UI 修改先读 `docs/REPAIROS_MOBILE_DETAIL_STANDARD.md`、`docs/UI_PAGE_GENERATION_DECLARATION.md`、`docs/COMPONENT_GENERATION_DECLARATION.md`、`docs/RESPONSIVE_DENSITY_PLAN.md`。
- 架构或跨模块修改先读 `docs/ARCHITECTURE.md`。
- 多代理任务按 `.agents/repairdesk-multiagent.yaml` 和 `.agents/task-package-template.md` 分配。
