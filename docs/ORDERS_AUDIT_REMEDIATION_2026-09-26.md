# 维修工单审计整改与发布验证（2026-09-26）

用户授权：修复和优化后推送并部署上线。实施基线 `dcd92316`，隔离分支 `codex/orders-audit-remediation-20260926`；不包含 canonical 工作区其他未提交改动。

## 已实施范围

| 原问题                       | 处理结果                                                                                                |
| ---------------------------- | ------------------------------------------------------------------------------------------------------- |
| B01 未报价显示尾款清         | 仅符合应收/结算状态时展示结清标记，取消订单单独处理                                                     |
| B02 批量状态变化覆盖新版本   | 单单及批量均强制原始版本、独立操作UUID，服务端拒绝重复工单/操作键                                       |
| B03 响应丢失后重复执行       | 客户端保留原始操作身份；服务端事务内读取回执并核验当前权限；未知400/5xx保持可安全重试                   |
| B04 工作流多步写入部分成功   | 默认状态切换和边集合替换改为原子RPC；锁等待后重新核验授权                                               |
| B05 清空设备备注无效         | 显式空字符串保留，不再回落到旧设备备注                                                                  |
| B06 恢复原值仍遗留离线草稿   | 序列化草稿清理，覆盖异步写入和回退原值竞态                                                              |
| L01 慢启动触发过早刷新       | 运行时正常启动等待15秒；明确脚本加载失败快速恢复；自动刷新有次数上限                                    |
| L02 打印依赖进入常用页面加载 | 捕获/合成时动态加载html2canvas/pdf-lib，保留3倍质量与可见打印恢复                                       |
| L03 三语字典进入共享脚本     | SSR只传当前语言，其他语言异步加载；竞态/失败不丢表单状态                                                |
| U01 报修故障入口不突出       | 手机前置故障描述；桌面/iPad提供常显字段并去除重复入口                                                   |
| U02–U04 布局、重复进度、触控 | iPad双栏和紧凑客户头；手机折叠低频信息；列表取消重复进度条；分类双按钮≥44px；桌面底栏固定且末尾内容可达 |

## 验证证据

本地证据目录：`artifacts/orders-remediation-20260926/`（含纯合成数据截图，不提交整套二进制产物）。

- 全量单元：云端CI的545个文件、5,179项全部通过；类型、lint、规则、风险覆盖与生产构建亦通过。浏览器分片继续按最终提交执行完整门禁。
- 风险覆盖：语句97.4%、分支87.75%、函数100%、行98.61%，达到现有阈值。
- PostgreSQL17隔离数据库：原子SQL约束/回滚/租户权限验证和9项真实双连接竞争通过；包含生命周期写保护与等待期间权限撤销。不是生产数据写入测试。
- Chromium：三语四宽度布局与草稿、8项启动恢复、10项工作台交互、7项批量失败恢复、移动录单触控、6个紧凑编辑代表流程、10项语言流程及8项现行打印流程通过。旧打印media测试按原有规则跳过。
- WebKit：8项四宽度布局/草稿交互及8项现行打印流程通过；原有旧打印用例跳过。
- 生产构建登录页无浏览器错误；3份完整字典独立块分别约237/247/261KB（未压缩文件大小），首屏未请求这些异步块；切换英文才请求英文块。当前语言通过SSR发送，不能将这些数字称作首屏传输量或速度提升百分比。
- 截图：`layout-final-results/`覆盖390中文、820意大利文、1180英文、1440中文的新建与详情页；`browser-contract-final-results/`覆盖紧凑分类编辑。

## 发布顺序与恢复

1. PR执行原有CI、E2E和新增的工单/恢复回归；不得忽略失败门禁。
2. 核对当前项目Supabase目标并先应用 `20260926093439_order_workflow_atomic_and_transition_receipts.sql`，再核验3个签名、invoker/空search_path、service_role执行授权和anon/authenticated拒绝。
3. 合并代码触发当前Vercel项目生产部署；以部署实际commit SHA和Ready状态核验，不以推送成功代替上线。
4. 验证正式域名登录、静态资源、语言切换及未登录受保护路由。出现构建/资源错误、租户权限回归或核心RPC不可用则停止推广，恢复已记录的前一Ready生产部署并保留加法RPC。
5. 本轮部署前恢复点：`dpl_AL43pYaaW3tRSUF47eD6aveNLpTp`；执行前须再次核对是否有其他发布。Vercel回滚可使用已核验部署ID；数据库无需删除函数或回填回退。旧应用恢复会重现其已知并发缺陷，必要时暂停相关写入并修复前进。

数据库复现命令和兼容说明见 [隔离数据库验证](../supabase/tests/orders_remediation_README.md)。稳定规则已同步 [数据一致性](REALTIME_DATA_CONSISTENCY_DECLARATION.md)、[启动与打印](STARTUP_PERFORMANCE_AND_PRINT_READINESS_DECLARATION.md) 和 [编辑标准](GLOBAL_CONTENT_EDITING_STANDARD.md)。

## 限制

Q01/Q02以本轮实际证据为准：未使用生产真实订单执行支付、客户消息或破坏性操作；未验证实体打印机和物理iPhone/iPad。曾出现webpack开发缓存下匿名SyntaxError与恢复循环，干净默认Turbopack开发服务及生产构建下正常；保留失败记录，不将开发资源故障记作功能通过。历史已丢失的设备空备注无法可靠回填。此次覆盖本轮工单审计问题，不承诺全站不存在未知缺陷。
